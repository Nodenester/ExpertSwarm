{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CSSSelector",
  "description": "CSS selectors for extracting content from web pages",
  "type": "object",
  "required": ["selectors"],
  "properties": {
    "selectors": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["selector", "target", "purpose"],
        "properties": {
          "selector": {
            "type": "string",
            "description": "CSS selector expression"
          },
          "target": {
            "type": "string",
            "enum": ["main_content", "title", "metadata", "navigation", "links", "code_block", "table", "list", "media"],
            "description": "What page element this selector targets"
          },
          "purpose": {
            "type": "string",
            "description": "Why this content is being extracted"
          },
          "extract_attribute": {
            "type": "string",
            "description": "HTML attribute to extract (null for text content)"
          },
          "multiple": {
            "type": "boolean",
            "description": "Whether to select all matches or just the first"
          }
        },
        "additionalProperties": false
      }
    },
    "page_type": {
      "type": "string",
      "description": "Detected page type (article, documentation, forum, etc.)"
    }
  },
  "additionalProperties": false
}
